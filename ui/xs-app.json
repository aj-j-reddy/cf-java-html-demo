{
    "logout": {
        "logoutEndpoint": "/do/logout"
    },
    "authenticationMethod": "none",
    "routes": [
        {
            "source": "/srv/(.*)",
            "destination": "services"
        },
        {
            "source": "/",
            "target": "/",
            "localDir": "webapp"
        }
    ]
}